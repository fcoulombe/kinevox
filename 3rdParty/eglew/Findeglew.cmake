
IF(${IS_EGLEW_ENABLED})
	IF(${WIN32})
		SET(EGLEW_ROOT "${CMAKE_CURRENT_LIST_DIR}")

		SET(EGLEW_INCLUDE_DIR ${EGLEW_ROOT}/include)

		FIND_LIBRARY(
  		EGLEW_LIBRARY 
  		NAMES 
    		eglew
  		PATHS
		${EGLEW_ROOT}/lib/${KINEVOX_ARCHITECTURE}
  		NO_DEFAULT_PATHS )
		SET(eglew_INCLUDE_DIR ${EGLEW_INCLUDE_DIR} )
		list(APPEND eglew_LIBS ${EGLEW_LIBRARY} )
	ELSE()
	ENDIF()

ENDIF()

