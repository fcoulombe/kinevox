FIND_PACKAGE(glew)

IF(${WIN32})
	IF(${IS_MSVC})
		SET(GLFW_ROOT "${CMAKE_CURRENT_LIST_DIR}")
		SET(LPATH ${GLFW_ROOT}/lib/${KINEVOX_ARCHITECTURE})
		SET(LIB_NAME "GLFW")
	ELSEIF(${IS_GNU})
		SET(GLFW_ROOT "${MSYS_PATH}/local")		
		SET(LPATH ${GLFW_ROOT}/lib/)
		SET(LIB_NAME "libglfw")
	ENDIF()
	
	SET(GLFW_INCLUDE_DIR ${GLFW_ROOT}/include)
	FindLibrary(GLFW_LIBRARY ${LIB_NAME} ${LPATH})
	
	SET(glfw_INCLUDE_DIR ${GLFW_INCLUDE_DIR} ${glew_INCLUDE_DIR})
	list(APPEND glfw_LIBS ${GLFW_LIBRARY} )
	list(APPEND glfw_LIBS ${glew_LIBS} )
	#message("glfw!!!" ${GLFW_LIBRARY})
ELSE()
        #FIND_PACKAGE(glfw)
        SET(glfw_INCLUDE_DIR ""  ${glew_INCLUDE_DIR})
        list(APPEND glfw_LIBS "glfw")
	list(APPEND glfw_LIBS ${glew_LIBS} )
ENDIF()

