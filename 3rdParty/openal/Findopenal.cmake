SET(OPENAL_ROOT "C:\\Program Files (x86)\\OpenAL 1.1 SDK\\")
FIND_PATH(
  OPENAL_INCLUDE_DIR al.h
  PATHS 
  "${OPENAL_ROOT}include")

IF(${KINEVOX_ARCHITECTURE} MATCHES "32bit")
	SET(OPEN_AL_PATH "${OPENAL_ROOT}libs\\Win32")
ENDIF()
IF(${KINEVOX_ARCHITECTURE} MATCHES "64bit")
	SET(OPEN_AL_PATH "${OPENAL_ROOT}libs\\Win64")
ENDIF()
FIND_LIBRARY(
  OPENAL_LIBRARY 
  NAMES 
    OpenAL32
  PATHS
    ${OPEN_AL_PATH}
  NO_DEFAULT_PATHS )

SET(openal_INCLUDE_DIR ${OPENAL_INCLUDE_DIR} )
list(APPEND openal_LIBS ${OPENAL_LIBRARY} )
#SET(openal_LIBS ${OPENAL_LIBRARY} )
