Import('env')

name = "sfml"
dependencies = []

def SetCFlags(ienv):
    if ienv['PLATFORM'] == "win32":
        ienv.Append(CPPPATH=[env.Dir('#3rdParty/sfml/include')])
    else:
        ienv.ParseConfig('pkg-config --cflags glfw')

def SetLFlags(ienv):
    if ienv['PLATFORM'] == "win32":
        ienv.Append(LIBS=[env.File('#3rdParty/sfml/lib/sfml.lib')])
        ienv.AppendUnique(LINKFLAGS=["/SUBSYSTEM:WINDOWS"])
    else:
        ienv.ParseConfig('pkg-config --libs sdl')
	
dllDep = []
env.AddComponent(name, None, None,None,dependencies, SetCFlags, SetLFlags, dllDep)
