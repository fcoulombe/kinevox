IF(${WIN32})
SET(ZLIB_ROOT "${PROJECT_SOURCE_DIR}/3rdParty/zlib/")
FIND_PATH(
  ZLIB_INCLUDE_DIR zlib.h
  PATHS ${ZLIB_ROOT}/include)


FIND_LIBRARY(
  ZLIB_LIBRARY 
  NAMES 
    zlib
  PATHS
    ${ZLIB_ROOT}/lib/${KINEVOX_ARCHITECTURE}
  NO_DEFAULT_PATHS )

SET(zlib_INCLUDE_DIR  ${ZLIB_INCLUDE_DIR})
list(APPEND zlib_LIBS ${ZLIB_LIBRARY})
#SET(zlib_LIBS  ${ZLIB_LIBRARY})
ELSE()
FIND_PACKAGE(ZLIB)
SET(zlib_INCLUDE_DIR ${ZLIB_INCLUDE_DIR})
list(APPEND zlib_LIBS ${ZLIB_LIBRARIES})

ENDIF()
