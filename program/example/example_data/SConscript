Import('env')
from Component import Component

name = "example_data"
data =  env.Dir("data")
dependencies = [ ]
incPath = []
lib = []

d = env.Install(env.Dir("$INSTALL_DIR/example"), data.srcnode())
ali = env.Alias("@example_data", d)

env.AddComponent(name, None, incPath, lib, dependencies)

